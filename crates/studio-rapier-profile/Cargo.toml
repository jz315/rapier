[package]
name = "studio-rapier-profile"
version = "0.1.0"
edition = "2024"
publish = false
license = "Apache-2.0"
repository = "https://github.com/jz315/rapier"

[lib]
crate-type = ["rlib"]

[dependencies]
rapier2d = { path = "../rapier2d", features = ["enhanced-determinism"] }
